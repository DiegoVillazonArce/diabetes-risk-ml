# Decision Log

This file records project decisions that affect scope, data handling, modeling, evaluation, deployment, or public communication.

## Decisions

| ID | Date | Status | Decision | Rationale | Consequences |
|---|---|---|---|---|---|
| D-001 | 2026-07-04 | Accepted | Keep the original Spanish blueprint outside the repository. | The blueprint is useful as an internal guide but is not structured as public project documentation. | Public repo documentation will be concise, English-first, and portfolio-oriented. |
| D-002 | 2026-07-04 | Accepted | Use English for repository-facing documentation. | The project is intended for recruiters, portfolio reviewers, and technical interviewers. | Docs should be written in clear English, while private planning conversations can remain Spanish. |
| D-003 | 2026-07-04 | Accepted | Use a lightweight Scrum-inspired workflow with rolling-wave planning. | ML work often changes after data inspection and model results, so future work should be refined progressively. | Roadmap stays high-level; backlog is updated frequently with user stories, tasks, and acceptance criteria. |
| D-004 | 2026-07-04 | Accepted | Treat the project as educational, not medical. | The dataset is self-reported survey data and the model is not clinically validated. | Disclaimers must appear in documentation and app output. |
| D-005 | 2026-07-04 | Accepted | Use the original imbalanced binary BRFSS dataset with `Diabetes_binary` as target. | The current local CSV matches the intended binary dataset: 253,680 rows, 218,334 negative cases, and 35,346 positive cases. | Modeling can proceed with the binary target. Evaluation data must preserve the class distribution selected by the documented data-preparation policy, rather than being balanced. |
| D-006 | 2026-07-05 | Accepted | Keep the raw CSV out of git for the MVP, while documenting the exact Kaggle source and CC0 license. | The selected dataset is CC0, so licensing is not the blocker; keeping it out of Git keeps the repository focused on code, docs, and lightweight deployable artifacts. | `dataset.csv` and raw/processed CSV outputs are ignored. Reproducers should download `diabetes_binary_health_indicators_BRFSS2015.csv` from Kaggle and place it at `data/raw/diabetes_binary_health_indicators_BRFSS2015.csv`. |
| D-007 | 2026-07-04 | Accepted | Train offline and serve serialized artifacts in Streamlit. | Streamlit should stay responsive and reproducible. | Training scripts generate artifacts; the app only loads and uses them. |
| D-008 | 2026-07-04 | Accepted | Start with a pinned `requirements.txt` for MVP dependency management. | This keeps the project easy for reviewers to run while still making package versions explicit. | A lockfile workflow such as `uv` can be evaluated later if reproducibility needs increase. |
| D-009 | 2026-07-04 | Accepted | Document manual Kaggle data acquisition before adding automation. | Kaggle API automation requires user credentials and can create friction for reviewers. | README or data docs must identify the dataset URL, required file, license, and expected local path; an optional download script can be added later. |
| D-010 | 2026-07-05 | Accepted | Use `joblib` for MVP model artifact serialization and evaluate `skops` before final packaging. | `joblib` is practical for scikit-learn pipelines and keeps the MVP simple, while `skops` may provide a safer artifact-loading story for a later hardening pass. | MVP artifacts will use `joblib` and should include local load/predict tests. This decision can be superseded if the project later adopts `skops`. |
| D-011 | 2026-07-04 | Accepted | Define focused pytest targets before implementation. | "Add tests" is too vague to guide development. | Initial tests should cover schema validation, feature ranges, stratified splits, pipeline predictions, and artifact loading. |
| D-012 | 2026-07-05 | Accepted | Use Python 3.12 with a hand-curated pinned `requirements.txt` as the first reproducible environment. | Python 3.12 is the locally installed default, has mature wheel support across the scientific Python stack, and is supported by Streamlit Community Cloud for the later deployment phase. Hand-curating top-level pins avoids a noisy `pip freeze` from the global environment. | `requirements.txt` pins numpy, pandas, scikit-learn, imbalanced-learn, joblib, matplotlib, seaborn, jupyterlab, streamlit, and pytest; `.python-version` records the interpreter version. The pins are validated when the first virtual environment is installed; any pin that fails to resolve should be adjusted individually, not unpinned. Unblocks US-0104. |
| D-013 | 2026-07-11 | Accepted | Distribute the trained model artifact to deployment as a controlled Git exception: exactly `models/diabetes_risk_model.joblib` is version-controlled, while every other artifact under `models/` stays git-ignored. | The P7 comparison of the three candidate paths (see the D-013 comparison section below) selected the Git exception on current evidence. The validated single-bundle artifact is ~263 KB (268,815 bytes) -- about 0.5% of GitHub's 50 MiB per-file warning threshold -- so versioning it costs almost nothing. Streamlit Community Cloud deploys by copying the repository files and running `streamlit run` from the repository root, and offers no pre-runtime build or download hook, so a committed artifact is the only path where the deployed app finds the model without network access or startup work. scikit-learn's persistence guidance requires a trusted, verified artifact source and an environment matching the training versions; versioning the artifact next to the exact code and pins that produced it, with the existing bundle validator checking model identity, hyperparameters, feature order, and classes at load time, satisfies both. A GitHub Release asset would force an in-app runtime download (network dependency at cold start, checksum/provenance management, and a violation of the project's local-only artifact-loading policy). Build-time generation is not possible on the platform (no build step exists), would need the git-ignored CSV or Kaggle credentials in the deployment environment, and running training inside the Streamlit process would break the D-007 offline-training/serving separation. | `.gitignore` unignores only the official artifact (`!models/diabetes_risk_model.joblib`); temporary (`*.tmp.joblib`) and alternative artifacts stay ignored. The official artifact is regenerated with Python 3.12 and the pinned `requirements.txt` versions and verified (metadata, model identity and hyperparameters, feature order, classes, smoke prediction) before being versioned. The app keeps loading only this local repository artifact and never trains, downloads, or reads the CSV at runtime (D-007). Each regeneration adds a ~263 KB blob to history, so regeneration stays tied to contract, seed, or dependency changes, and byte-identical regeneration is not expected (metadata records a creation timestamp); behavioral reproducibility (same seed, data, and pins produce the same predictions and metrics) is the contract. Docstrings and tests that assumed the artifact is always git-ignored are updated. Verification: local clean-environment check in P7, then confirmation in the Streamlit Community Cloud environment during the US-0703 deployment. |
| D-014 | 2026-07-06 | Accepted | Keep exact duplicate rows for MVP data preparation. | The P2 EDA found 24,206 exact duplicate rows (~9.5%), skewed heavily toward the negative class; dropping them would shift positive prevalence from ~13.9% to ~15.3%. Because the Kaggle file is a reduced, low-cardinality survey dataset without respondent identifiers, exact duplicate rows plausibly represent distinct respondents with the same observed health-indicator profile rather than data-entry errors. | P3 must retain the full 253,680-row dataset before splitting and preserve the observed ~13.9% positive prevalence in train, calibration, and test splits. Duplicate removal can be revisited as a post-MVP sensitivity analysis, but it is not the default preparation path. |
| D-015 | 2026-07-07 | Accepted | Return P3 train/calibration/test splits in memory instead of writing files to `data/processed/`. | The split logic lives in a reusable module (`src/data.py`) with a fixed random seed, so any consumer can recreate identical splits on demand. No downstream consumer needs persisted split files yet, and writing them now would add artifact management (naming, regeneration, staleness) without a benefit. | `split_data()` and `prepare_data()` return in-memory DataFrames and `data/processed/` stays empty for now. Revisit if P4 training scripts or deployment need persisted splits; any future file output must document its format and regeneration path first. |
| D-016 | 2026-07-09 | Accepted | Select the P5 `HistGradientBoostingClassifier` candidate (library defaults, fixed seed) as the primary MVP model. | P5 compared Dummy, Logistic Regression, a `class_weight="balanced"` Logistic Regression variant, and `HistGradientBoostingClassifier` on the same P3 splits with the documented metric protocol. The tree-based candidate leads the primary ranking metric, PR-AUC (0.423 vs. 0.394 for Logistic Regression and 0.393 for the balanced variant), also has the strongest ROC-AUC (0.827 vs. 0.819/0.820) and best default-threshold positive-class precision (0.563), and keeps a small train/test PR-AUC gap (0.032, under the 0.10 overfitting limit). The balanced variant has the best default-threshold recall (0.760) and F1 (0.441), but does not improve PR-AUC or ROC-AUC; honest probability ranking matters more for a risk-estimation app, and threshold/calibration work belongs to P8. | Later phases (P6 Streamlit serving, P8 calibration, P9 SHAP) build on `HistGradientBoostingClassifier`. Artifact serialization follows the D-017 timing with the D-010 `joblib` format, and D-013 still governs deployment distribution. The low default-threshold recall of the selected unweighted model is a documented limitation for the P8 threshold analysis. |
| D-017 | 2026-07-09 | Accepted | Defer model artifact serialization to the start of P6 (Streamlit MVP). | P5 compared and selected models fully in memory; no P5 consumer needs a persisted artifact, and writing one before its consumer exists would create an untested artifact-management path. The first real consumer is the P6 Streamlit app, whose plan already includes a load/predict smoke check (US-0503). | The D-016 model will be serialized during P6 using the D-010 `joblib` format together with a local load/predict verification; `models/` stays empty until then, and D-013 still governs how the artifact reaches deployment before the first public deployment in P7. |

## D-013 Artifact Distribution Comparison (P7, 2026-07-11)

Formal comparison behind the D-013 acceptance, recorded before the first public deployment (US-0702). The evaluated artifact is the P6 single-bundle `models/diabetes_risk_model.joblib` (~263 KB, fitted D-016 model plus serving metadata). Sources consulted on 2026-07-11: Streamlit Community Cloud documentation ([app dependencies](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/app-dependencies), [file organization](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/file-organization), [deploy settings](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/deploy)), [scikit-learn model persistence](https://scikit-learn.org/stable/model_persistence.html), and [GitHub large-file limits](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github).

Platform facts that frame the comparison: Community Cloud copies the repository files and runs `streamlit run` from the repository root on Debian Linux; dependencies install from `requirements.txt`; there is no pre-runtime build step, hook, or arbitrary script execution; Python 3.12 is the platform default version. scikit-learn supports loading persisted models only from trusted, verified sources into an environment with the same library versions. GitHub warns at 50 MiB per file and blocks 100 MiB.

| Criterion | A. Controlled Git exception (selected) | B. GitHub Release asset | C. Generation during deployment build |
|---|---|---|---|
| Artifact size | ~263 KB: ~0.5% of GitHub's 50 MiB warning threshold; negligible clone cost. | Fine (releases target large files), but the size benefit is moot at 263 KB. | No stored artifact, but the 22.7 MB git-ignored CSV would be needed instead. |
| Reproducibility | Official artifact is pinned in history next to the exact code and pins that produced it; regenerable via `python -m src.artifacts` (fixed seed, pinned versions). | Asset must be built, uploaded, and kept in sync with the repo manually; drift between repo pins and released artifact is possible. | Would retrain per deploy; deterministic in theory (fixed seed) but unverifiable on this platform because no build step exists. |
| Security / provenance of the joblib | Provenance is the repository itself (trusted source per scikit-learn guidance); load-time validator checks model class, hyperparameters, feature order, and classes. | Requires download plus checksum/provenance verification logic; the project's durable local-only loading policy forbids remote artifact sources in the app. | No download, but training code would run inside the serving process with secret Kaggle credentials present. |
| Version compatibility | Artifact and `requirements.txt` travel in the same commit; metadata records Python/numpy/pandas/scikit-learn/joblib versions for verification. | Released artifact can lag behind repo pin changes; compatibility must be re-checked out of band. | Always trained with installed versions -- the one real advantage, unusable here. |
| Git history impact | ~263 KB per regeneration; acceptable while regenerations stay tied to contract/dependency changes. | None (clean history). | None. |
| Maintenance | Lowest: one file, one workflow, no extra infrastructure. | Release workflow, tagging, checksums, and sync discipline per model update. | Kaggle credential management, download scripting, and platform workarounds. |
| Network dependency | None beyond cloning the repo (which deployment does anyway). | Runtime download from GitHub on cold start; outage or rate limit breaks startup. | Runtime download of the CSV from Kaggle (authenticated) on cold start. |
| Cold start | Fastest: file already present when the app starts. | Slower: fetch + verify before first prediction. | Slowest: download 22.7 MB CSV + train (~minutes) before first prediction. |
| CSV availability | Not needed at deploy time; training stays local/offline. | Not needed at deploy time. | Blocker: the CSV is git-ignored (D-006) and absent from the deployment copy; obtaining it needs Kaggle credentials as platform secrets. |
| Streamlit Community Cloud viability | Native fit: the platform copies repository files, so the committed artifact simply arrives. | Only via in-app runtime download, since no pre-runtime hook exists. | Not viable: no build step exists, so "build-time" generation would actually run inside the Streamlit serving process. |
| Build/training vs. runtime serving separation | Preserved: training remains an offline local step (D-007); the app only loads a file. | Preserved for training, but adds a runtime distribution step inside the app. | Broken: training would execute in the serving process at startup. |

Alternative C is rejected because the platform offers no reproducible, secure pre-runtime mechanism (the mandate's own rejection criterion), it would require the git-ignored CSV or credentials at deploy time, and it would break the offline-training/serving separation. Alternative B is rejected because, without a build step, the download must happen inside the app at runtime -- adding a cold-start network dependency and checksum/provenance machinery that the project's local-only artifact-loading policy explicitly forbids, to solve a distribution problem that a 263 KB file does not have. Alternative A is selected: it is the only alternative that needs no new mechanism, keeps provenance in the repository, keeps the app local-only, and matches how the platform actually delivers files.

## Status Values

- **Proposed:** under discussion.
- **Accepted:** active decision.
- **Rejected:** considered but not used.
- **Superseded:** replaced by a later decision.
- **Pending:** decision required before dependent work continues.
